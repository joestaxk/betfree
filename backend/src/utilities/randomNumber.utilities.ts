


export function RandomNumber(numb: number) {
    return Math.floor(Math.random() * numb)
}


export function RandomPerfect(numb: number, ) {
    let lastNum;
    let rand = RandomNumber(numb);
    
}